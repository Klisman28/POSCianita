import { createSlice } from '@reduxjs/toolkit'

const newSlice = createSlice({
    name: 'catalogue/configs/data',
    initialState: {
        
    },
    reducers: {},
    extraReducers: {
        
    }
})

export default newSlice.reducer
